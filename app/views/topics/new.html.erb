<h2>New Topic</h2>
<div class="form">
	<%= form_for [@user, @topic] do |f| %>
		<%= f.label :title, "Title:" %>
		<br>
		<%= f.text_field :title %>
		<br>
		<%= f.label :body, "Content:" %>
		<br>
		<%= f.text_area :body %>
		<br>
		<%= f.submit "Post", :class => "submit" %>
	<% end %>
</div>